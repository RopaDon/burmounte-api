export enum Controllers {
  App = "/app",
  Auth = "/auth",
  News = "/news",
  Users = "/users",
  Search = "/search",
  WatchList = "/watch-list",
  Currencies = "/currencies",
  Notifications = "/notifications",
  Subscription = "/susbcriptions",
  ActiveSymbols = "/active-symbols",
  TransactionMethods = "/transaction-methods",
}
